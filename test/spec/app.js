define(['app'], function (App) {
    "use strict";

    describe("App", function () {
        it(
            "should be defined",
            function () {
                expect(App).toBeDefined();
            }
        );
    });
});
